# Email Scraper Termux

Un outil simple pour extraire des adresses e-mails depuis des pages web.

## ✅ Prérequis (dans Termux)

```bash
pkg update && pkg upgrade
pkg install python git -y
pip install requests beautifulsoup4
